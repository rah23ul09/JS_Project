/*
    1. Create an array
    2. Add an elements in existing array
    3. Retrieve elment from array
    4. Concept of length
*/

// Create an array
let a = [10, 20, 30, 40, 50, 60]
let b = ["Rahul", "Singh"]

console.log(a)
console.log(b)

console.log("------------------------------------")

// Add an element in array
a[0] =100
b[0] = "Vicky"
console.log(a)
console.log(b)

console.log("------------------------------------")

// length of array
console.log(a.length)
